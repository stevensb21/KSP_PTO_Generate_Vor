# Модели шаблонов больше не используются
# Шаблоны теперь - это типы работ (WorkType) со списком работ (WorkTypeWork) и ресурсов (WorkResource)
# Эти модели оставлены для совместимости, но не используются

from django.db import models
